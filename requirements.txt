# HTTP-клиент к OpenDota — нужен всегда (и CLI, и вебу).
requests>=2.31

# Локальный веб-интерфейс. Для CLI не обязательны: без них работает
# `python -m dota2coach analyze`, недоступна только команда `serve`.
fastapi>=0.115
uvicorn>=0.32
